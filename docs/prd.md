# PRD — above (MVP)

- 작성일: 2026-07-26 · 갱신: 2026-07-28 · 상태: **확정** (미결 없음 — M3 검증 단계 진입, Vercel 배포 완료)
- 참고 시안: https://frame-collage--heesunyou.replit.app/above/ (바이브코딩 프로토타입)

## 1. 문제와 기회

인스타그램에 올릴 사진을 "꾸며서" 올리고 싶은 수요는 크지만, 기존 도구(포토샵, 범용 편집 앱)는
과하게 복잡하다. 템플릿을 고르고 → 사진을 넣고 → 다운받는 3단계로 끝나는 초경량 도구로
이 수요를 검증한다.

## 2. 검증 가설

> **H1. 인스타 감성 프레임을 씌워주는 도구가 있으면, 방문자가 실제로 결과물을 다운받아 쓴다.**

- 1차 지표: **방문 → 다운로드 전환율. 목표 25%** (확정 2026-07-27 — 로그인 등 마찰 요소가 없는 초경량 도구임을 감안해 상향. 초기 데이터 보고 조정)
- 보조 지표: 방문자 수, 템플릿별 편집 사용·다운로드 분포, Post/Story 비율 선택 분포
- 측정 도구: **Vercel Analytics** (확정 2026-07-27)
  - 커스텀 이벤트는 Pro 전용이므로 무료(Hobby) 범위에서 **라우트 기반으로 측정**한다:
    템플릿별 편집 사용 = `/editor/[frameId]` 페이지뷰, 다운로드 = 내보내기 완료 화면
    라우트(예: `/editor/[frameId]/done`) 페이지뷰로 집계. 정밀 측정이 필요해지면 Pro 또는 GA4 병행 검토

## 3. 타깃 사용자

**인스타그램 헤비유저 10–20대.** 피드/스토리를 꾸며서 올리는 데 익숙하고,
폴라로이드·낙서·주간 덤프 같은 감성 템플릿 문법을 이미 소비하고 있는 층.
주 접점은 모바일 브라우저 (인스타 인앱 브라우저 포함 — 다운로드 동작 확인 필요).

## 4. MVP 범위

**포함**

- 템플릿 리스트 (홈): 카드형 미리보기, 지원 비율 표기
- 에디터: 슬롯 탭 → 사진 첨부, 슬롯 안에서 드래그·핀치줌으로 위치 조정, Post 4:5 / Story 9:16 전환
- PNG 다운로드 (Post 1080×1350, Story 1080×1920)
- 모바일 웹 최적화 (데스크톱은 중앙 고정 컬럼)

**제외 (검증 후 판단)**

- 계정/로그인, 서버 저장, 공유 링크, 결제
- 커스텀 프레임 제작, 텍스트 입력, 필터/보정

## 5. 템플릿 라인업

**확정 (2026-07-27): 디자이너 시안 6종 × Post/Story 2비율 = 12장** — `docs/design/frames/`에 원본 보관
(Frame01–06 + 사용 예시 Sample01–06, 원본 2160×2700 / 2160×3840).

| ID      | 가칭        | 구조 요약                                                                        |
| ------- | ----------- | -------------------------------------------------------------------------------- |
| frame01 | Duo         | 남색 텍스처 배경 + 세로 슬롯 2개 (사진 2장)                                      |
| frame02 | Punching    | 서로 다른 사진 2장 — 하단 사각 + 상단 별무리 구멍, 파란 별 장식이 사진 위에 얹힘 |
| frame03 | Accent      | 독립 슬롯 2개 — 상단 소형 + 하단 대형                                            |
| frame04 | Weekly Dump | 요일 라벨 7슬롯 그리드 (사진 최대 7장), 라벨·그라디언트가 사진 위에 얹힘         |
| frame05 | Doodle      | 중앙 슬롯 1개, 초록 낙서 스트로크가 사진 경계를 넘어 위에 얹힘                   |
| frame06 | Caption     | 상단 슬롯 1개 + 하단 red 텍스처·캡션 타이포                                      |

시안은 슬롯 영역을 회색 자리표시(불투명)로 표기한 통짜 PNG다. 런타임 에셋(사진 아래 배경 / 사진 위
오버레이)은 자리표시 색 제거로 파생 생성한다 — 상세는 [스펙 01](specs/01-template-schema.md).
템플릿은 JSON 스키마 + 에셋으로 정의한다 (AGENTS.md 원칙).

## 6. 기능 스펙

기능별 상세 스펙은 `docs/specs/`에 작성한다. 예정 목록:

| 스펙                                    | 상태        |
| --------------------------------------- | ----------- |
| 01 템플릿 스키마 (JSON 구조, 에셋 규약) | Implemented |
| 02 템플릿 리스트 (홈)                   | Implemented |
| 03 에디터 (슬롯·비율·조작)              | Implemented |
| 04 내보내기 (PNG 다운로드)              | Implemented |
| 05 사진 자유 회전                       | Implemented |
| 06 선택 기반 편집 (탭 선택·오버레이)    | Implemented |

## 7. 마일스톤

1. **M1 — 코어**: 템플릿 스키마 확정 + 에디터 코어(슬롯/조작/내보내기) — 템플릿 1종으로 관통
2. **M2 — 라인업**: 사용자 첨부 시안을 스키마로 이식, 전 템플릿 적용
3. **M3 — 검증**: Vercel 배포 + 지표 수집 시작

## 8. 미결 사항

- [x] 템플릿 라인업/에셋 첨부 — 2026-07-27 완료 (`docs/design/frames/`)
- [x] 전환율 목표치 확정 — 25% (2026-07-27)
- [x] 분석 도구 선정 — Vercel Analytics (연결은 사용자 진행)
- [x] 서비스명 "above" 확정 (2026-07-28)
